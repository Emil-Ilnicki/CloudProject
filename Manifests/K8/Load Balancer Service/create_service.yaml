apiVersion: v1
kind: Service
metadata:
  name: distributed-project-service
  annotations:
    service.beta.kubernetes.io/linode-loadbalancer-throttle: "4"
  labels:
    app: distributed-project
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: distributed-project
  sessionAffinity: None
